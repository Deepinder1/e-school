//this is going to be a simple class

class User {
  //we are going to define what properties users have

  final String uid;

  User({this.uid}); //creating a constructor an d passing this uid

  //after this we would convert firebase user object into
  //into user object made under this class
}
